# Sylveon concise notes

- Purpose: transform a heatmap into structured/distributed code-context categorization.
- Balance rule: `65%` logic (backend architecture) + `35%` pattern (frontend design).
- Entry flow: heat-spectrum entry point -> hook/probe -> `basepyright` context injection -> parallel todo pull.
- Compass add-on (lego style): V-to-| spine transform + left/right tail hooks + sharp needle tip.
- Signal semantics:
  - Red long horizontal lanes: sharp probing data, fast parallel transport.
  - Lime phosphorescent spread: ornamental/global coverage and broad contextual ambience.
- Drift protection: open point-selection strategy (multiple hotspots) to avoid locking one region.
- Guidance cadence: 4-6 second intervals; markers indicate next center point.
- Output intent: actionable recommendation material + textbook code best practices.
- Recommendation framing:
  - high: type contracts, narrow interfaces, deterministic tests
  - medium: naming clarity, docstring precision, explicit side-effects
